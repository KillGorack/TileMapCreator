<Window x:Class="TileMapCreator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TileMapCreator"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        SizeChanged="Window_SizeChanged">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" Background="LightGray">

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="120"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0">
                <Label Content="Border map" FontWeight="Bold" FontSize="16" Margin="5"/>
                <Image Width="140" Height="128" Name="Border_Map" Margin="5"/>
                <Label Content="Tile map" FontWeight="Bold" FontSize="16" Margin="5"/>
                <Image Width="140" Height="128" Name="Tile_Map" Margin="5"/>
            </StackPanel>


            <StackPanel Grid.Row="1">
                <Button Content="Add Boder Map" Margin="5" Click="Add_Border_Click"/>
                <Button Content="Add Tile Map" Margin="5" Click="Add_Tile_Click"/>
                <Button Content="Remove Maps" Margin="5" Click="Remove_Click"/>
                <Button Content="Generate Map" Margin="5" Click="BuildTileset_Click"/>
            </StackPanel>
            
        </Grid>

        <StackPanel Grid.Column="1" Background="LightBlue">
            <Label Content="Border size" FontSize="12" Margin="5, 5, 5, 0"/>
            <TextBox Name="Border_Size" Margin="5, 0, 5, 5" TextChanged="TextChanged"/>
        </StackPanel>

        <Grid Grid.Column="2">
            <Grid Background="White">
                <Image Name="Edit_Screen"
               SnapsToDevicePixels="True"
               RenderOptions.BitmapScalingMode="NearestNeighbor"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"/>

                <Canvas Name="GridOverlay"
                IsHitTestVisible="False"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
